# Database: use SQLite for local dev, or PostgreSQL for production
# SQLite (default, no install needed):
DATABASE_URL="file:./dev.db"

# PostgreSQL (optional, uncomment and set):
# DATABASE_URL="postgresql://user:password@localhost:5432/torkiaparis"

# Server port
PORT=3001
