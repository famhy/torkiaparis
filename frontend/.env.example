# Base URL of the backend API (no trailing slash; /api is appended by the app)
VITE_API_BASE=http://localhost:3001/api

# Admin login password for /admin (session in sessionStorage)
VITE_ADMIN_PASSWORD=admin
